# Block 20 Group Website

## Setup

Run `yarn install` the first time after cloning the repo

## Development

Run `yarn dev` to start the development server

## Build

Run `yarn build` to compile a static build at `./docs`
